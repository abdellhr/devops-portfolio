global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "devops-demo-app"
    static_configs:
      - targets: ["app:5000"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
